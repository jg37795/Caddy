/* sw.js — offline-first service worker for Caddy. */
const CACHE_VERSION = 'v1.1.4'; // + HOLE VIEW REBUILD: numeric camera fit (was a hardcoded guess that dropped 2849/9025 quads off-frame at high exag); fairway brightened; arrows sparser + void-parity + off-surface gate (none float over background); tee-to-green yardage + aim line when a tee is known
